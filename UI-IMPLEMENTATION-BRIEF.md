# בריף מימוש — Optimal Motion UI Direction

מסמך הזה נועד להעתקה ישירה לתוך Claude Code ב-VS Code, על הריפו האמיתי (`optimal-motion`), כדי לממש את כיוון העיצוב שאושר.

**קנבס העיצוב (8 מסכים, מובייל 390×844, RTL):**
https://claude.ai/code/artifact/bbc33ba3-e29f-4fe7-9328-796d1b8d9c6d

לפני שמתחילים — לפתוח את הקישור ולהסתכל על כל 8 המסכים: Main, WorkoutActive, WorkoutRest, Login, PremiumStore, Profile, DiyBuilder, MyWorkouts. אלה מוקאפים סטטיים (HTML/CSS גולמי, לא קוד React) — המטרה היא **להתאים את המבנה, המרווחים וההיררכיה הוויזואלית שלהם** לתוך הקומפוננטות האמיתיות, לא להעתיק HTML.

---

## חוקי ברזל

1. **Zero-breaking-changes**: לא למחוק או לשכתב לוגיקה עסקית קיימת (state management, קריאות ל-Supabase, חישובים) בלי סקירה מפורשת. זה שינוי ויזואלי/UX — לא רפקטור.
2. לעבוד **מסך אחד בכל פעם**: לממש, להריץ מקומית ולהסתכל בדפדפן, ואז לעבור הלאה. לא לנסות את כל 8 המסכים בפעם אחת.
3. שני מסכים (DiyBuilder + MyWorkouts) דורשים בנוסף **שכבת דאטה חדשה**, לא רק CSS — מפורט בהמשך תחת "שכבת דאטה חדשה".
4. לשמור על RTL מלא (`dir="rtl"`, כיווני חצים נכונים) ועל גופן Rubik הקיים.

---

## Design tokens שנקבעו

```css
--bg-app: #0c0a09;      /* stone-950, רקע בסיס */
--card: #1c1c1e;
--border: #292524;
--teal: #14b8a6;
--teal-light: #2dd4bf;   /* אקסנט פונקציונלי/מותג — נשאר בכל המסכים */
--amber: #f59e0b;
--amber-light: #fbbf24;
--orange: #ea580c;
--emerald: #10b981;
--emerald-light: #34d399; /* ייחודי למסך המנוחה בין סטים */
```

תוספות ל"גל החם" (Main + Login בלבד — שאר המסכים נשארים על טיל כאקסנט):
```css
coral: rgba(248,113,86,1)   /* טקסט: #f87156 */
gold/olive: #facc15         /* rgba(234,179,8,x) לרקעים */
terracotta: #fdba74         /* rgba(251,146,60,x) לרקעים */
```

תגיות קטגוריה חדשות (DiyBuilder + MyWorkouts):
```css
קטלבל:    #f59e0b  /* rgba(245,158,11,x) */
יוגה:      #f87156  /* rgba(248,113,86,x) */
שרירים:    #60a5fa  /* rgba(59,130,246,x) */
מוביליטי:  #facc15  /* rgba(234,179,8,x) */
```

גופן: Rubik (כבר קיים ב-`app/layout.tsx` דרך `next/font/google`) — אין צורך לשנות.

---

## מיפוי מסך → קובץ אמיתי

| מוקאפ | קובץ אמיתי | סוג שינוי |
|---|---|---|
| Main.dc.html | `app/components/patient/tabs/PlanTab.tsx` | ויזואלי בלבד |
| WorkoutActive / WorkoutRest | `app/components/patient/workout/WorkoutPlayer.tsx` + `app/hooks/useWorkoutSession.ts` | **ויזואלי + שינוי UX בזרימת הקלט** |
| Login.dc.html | `app/components/marketing/LoginPage.tsx` (+ `LandingPage.tsx` לרקע) | ויזואלי בלבד |
| PremiumStore.dc.html | `app/components/patient/tabs/PremiumStoreTab.tsx` | ויזואלי בלבד |
| Profile.dc.html | `app/components/patient/tabs/ProfileTab.tsx` | ויזואלי + תיוג עברית לתוויות סטטיסטיקה |
| DiyBuilder.dc.html | `app/components/patient/tabs/DiyBuilderTab.tsx` | ויזואלי **+ פיצ'ר חדש** (קטגוריות, שמירה) |
| MyWorkouts.dc.html | מסך/route חדש לגמרי | **פיצ'ר חדש** |

---

## הערות ספציפיות למסך

### Main (PlanTab.tsx)
- אווירה חמה יותר: גרדיאנט hero בגוונים חמים (ענבר/נחושת) במקום הכהה הקר, אווטאר בענבר.
- הסרת נתונים מזויפים שכבר אותרו באודיט: "45 דקות" קשיח, "לכל הרמות" קשיח, `fakeProgressPercent`.
- כרטיסי קטגוריה (יוגה/מוביליטי) בגוונים חמים תואמים.

### WorkoutActive + WorkoutRest (WorkoutPlayer.tsx, useWorkoutSession.ts)
זה השינוי המשמעותי ביותר ב-UX, לפי בקשה מפורשת:
- **להעביר** את הזנת מספר החזרות מהמסך הפעיל למסך המנוחה, יחד עם הזנת RIR.
- במסך הפעיל: להראות רק "X חזרות · סט Y/Z" כפילה קומפקטית קטנה (לא readout גדול במרכז) בתוך שורת הכפתורים הקיימת (info/קושי), לא כאלמנט נפרד.
- **להסיר** את בלוקי ה-Block מהמסך הפעיל.
- **להחליף** את כפתור "החלף תרגיל" בשתי פונקציות: הפוך לקל יותר / הפוך לקשה יותר.
- כפתור פעולה ראשי יחיד וצף (לא בתוך כרטיס) בתחתית המסך הפעיל.
- מסך המנוחה: stepper לחזרות + שורת RIR (0/1/2/3/4+) בכרטיס אחד, טיימר טבעת 176px בירוק אמרלד, "הבא בתור" כשורה קומפקטית.
- ⚠️ תזכורת מהאודיט: יש כבר באג ידוע ב-`useWorkoutSession.ts` (`exTimer` לא נזרע — טיימר תרגיל שבור). זה **לא** חלק מהעבודה הוויזואלית הזו — לתעדף בנפרד, אבל שווה לציין ל-Claude Code שלא "לתקן בטעות" תוך כדי מעבר בקובץ בלי לבדוק.

### Login (LoginPage.tsx / LandingPage.tsx)
- להחליף רקע תמונת סטוק גנרית (Unsplash) בגרדיאנט מצויר חם — גם פתרון ויזואלי וגם מטפל בכפילות תמונת הרקע שחוזרת בכמה מסכים.
- כרטיס login לבן/כמעט-לבן עם shadow עמוק, ולידיות בקונטרסט תקין (`#57534e`, לא `#a8a29e` שדהוי מדי על רקע בהיר).

### PremiumStore (PremiumStoreTab.tsx)
- 3 כרטיסי מסלול: קליסטניקס (בבעלות, טיל), יוגה (נעול, ענבר), קטלבל (נעול, ענבר) — לפי התיאורים האמיתיים ב-`catalog.ts`.
- ⚠️ לוודא כיוון חץ נכון ב-RTL על כפתור "עבור לאימון" (חץ "קדימה" ב-RTL מצייר שמאלה, לא ימינה).

### Profile (ProfileTab.tsx)
- תוויות סטטיסטיקה בעברית (ימי רצף / דרגה / אימונים) במקום אנגלית קשיחה בקוד.
- שם הדרגה עצמו (Silver/Gold/וכו') נשאר באנגלית — זה ערך דאטה מהסכמה, לא label של ממשק.
- לוודא שכיוון הכפתור/מתג הרטט (haptics toggle) תואם ללוגיקה האמיתית הקיימת (`hapticsEnabled ? "left-1" : "right-1"`) — לא לשנות התנהגות, רק לוודא שהעיצוב תואם אותה.

### DiyBuilder (DiyBuilderTab.tsx) — כולל פיצ'ר חדש
- שורת פילטר קטגוריות מעל הקטלוג: הכל / קטלבל / יוגה / שרירים / מוביליטי.
- תגית קטגוריה צבעונית על כל תרגיל בקטלוג (ראה טבלת צבעים למעלה).
- כפתור משני "שמור אימון" ליד הכפתור הקיים "התחל אימון עכשיו".
- כפתור/קישור "האימונים שלי" בכותרת המסך, מוביל למסך החדש.

### MyWorkouts — מסך/route חדש לגמרי
- רשימת כרטיסי אימונים שמורים: שם, יום, תגיות קטגוריה (מספר תרגילים לכל קטגוריה), כפתורי "התחל אימון" / "ערוך".
- כפתור "בנה אימון חדש" בתחתית, חוזר ל-DiyBuilder.
- דורש ניווט חדש (טאב/route) — להחליט איפה זה יושב ב-`PatientShell.tsx` (טאב נפרד, או נגיש רק מתוך DIY Builder — כרגע המוקאפ מניח שהוא נגיש רק מ-DIY Builder ולא מה-bottom nav הראשי).

---

## שכבת דאטה חדשה נדרשת (DiyBuilder + MyWorkouts)

בשונה משאר המסכים, שני אלה הם **פיצ'ר אמיתי, לא רק פוליש**:

1. **קטגוריה לתרגיל**: כל תרגיל צריך שדה קטגוריה (אחד מתוך 4 ערכים קבועים). לבדוק אם יש כבר שדה מתאים בסכמת Supabase / ב-`app/types/index.ts`, ואם לא — להוסיף עמודה/enum. **לא להמציא סכמה בלי לבדוק קודם את המבנה הקיים** (יכול להיות שיש כבר taxonomy דומה דרך `ADMIN_TAGS`/`AVAILABLE_MUSCLES` ב-`app/constants/catalog.ts`).
2. **שמירת אימון DIY**: צריך טבלה/מודל חדש (למשל `patient_saved_workouts`) לשמור אימון שנבנה: שם, יום, רשימת תרגילים, timestamps. לבדוק RLS policies מתאימות (Supabase Row Level Security) כדי שכל מטופל יראה רק את האימונים שלו.
3. מומלץ ש-Claude Code **יעצור ויציג הצעת סכמה לפני שהוא כותב מיגרציה** — זה שינוי מבני, לא רק ממשק.

---

## תזכורת: באגים שכבר זוהו באודיט (לא חלק מהעבודה הזו)

לא לגעת בהם עכשיו, אבל חשוב שיישארו על הרדאר להמשך:
- סיסמאות בטקסט גלוי + login מנהל hardcoded (`admin`/`admin`)
- `email_verified` אף פעם לא מתעדכן ל-`true` — חוסם רכישות פרימיום לצמיתות
- טיימר תרגיל שבור (`exTimer` לא נזרע ב-`useWorkoutSession.ts`)
- אין גבול auth בצד שרת (client Supabase יחיד עם anon key)

---

## תהליך עבודה מומלץ ל-Claude Code

1. לפתוח branch נפרד לעבודת העיצוב (`git checkout -b ui-direction-patient`).
2. לעבור מסך-מסך לפי הטבלה למעלה, בסדר: Main → WorkoutActive/Rest → Login → PremiumStore → Profile → DiyBuilder → MyWorkouts (מהפשוט למורכב; MyWorkouts אחרון כי הוא תלוי בשכבת הדאטה).
3. אחרי כל מסך: `npm run dev`, לבדוק ויזואלית מול המוקאפ המתאים בקנבס, commit.
4. ל-DiyBuilder + MyWorkouts: קודם לסכם את שכבת הדאטה (schema/migration), רק אז לממש UI.
5. בסוף: `npm run lint` + `npm run build` לוודא שלא נשבר כלום.
